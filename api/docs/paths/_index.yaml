# ---------- Auth ----------
/auth/login:
  $ref: 'auth/authLogin.yaml'

# ---------- Domains ----------
/domains:
  $ref: 'domains/domains.yaml'
/domains/{domainId}:
  $ref: 'domains/domainsById.yaml'
/domains/{domainId}/version/{version}:
  $ref: 'domains/domainsDelete.yaml'
/domains/selector:
  $ref: 'domains/domainsSelector.yaml'

# ---------- SettlementDeeds ----------
/settlementDeeds:
  $ref: 'settlementDeeds/settlementDeeds.yaml'
/settlementDeeds/{id}:
  $ref: 'settlementDeeds/updateSettlementDeedsById.yaml'
/createSettlementDeed:
  $ref: 'settlementDeeds/createSettlementDeeds.yaml'
/settlementDeeds/{settlementId}:
  $ref: 'settlementDeeds/getSettlementDeeds.yaml'
/settlementDeeds/{deleteSettlementId}:
  $ref: 'settlementDeeds/deleteSettlementDeeds.yaml'